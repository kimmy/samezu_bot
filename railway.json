{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "apt-get update && apt-get install -y libasound2t64 libffi8 && playwright install chromium && python run_bot.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  },
  "service": {
    "name": "samezu-bot",
    "serviceType": "background"
  }
}
