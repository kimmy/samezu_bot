[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live: optional live-site Playwright smoke (set LIVE_SCRAPE=1; excluded from deploy by default if you use -m "not live")
# Playwright fixture tests use session fixture chromium_available (skip if browser missing)
