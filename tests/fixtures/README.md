# HTML fixtures

Saved calendar markup for parser tests (Phase D). Not used by live Playwright checks yet.

| File | Description |
|------|-------------|
| `tokyo_calendar_sample.html` | Tokyo table snippet with `aria-label="予約可能"` (from former `test_html_structure.py`) |

Add `kanagawa_calendar_sample.html` when a stable sample is captured from production.
