# Telegram Bot Configuration
# Get your bot token from @BotFather on Telegram
TELEGRAM_BOT_TOKEN=your_bot_token_here

# User Configuration (managed via manage_users.py script)
# Each user can have different notification preferences:
# - notify_no_slots: Send "no slots" messages (True/False)
# - notify_slots: Send detailed slot notifications (True/False) 
# - notify_errors: Send error notifications (True/False)

# Example configuration in config.py:
# TELEGRAM_USERS = {
#     "your_chat_id_here": {
#         "name": "Your Name",
#         "notify_no_slots": True,   # Send simple "no slots" messages
#         "notify_slots": True,       # Send detailed slot notifications
#         "notify_errors": True       # Send error notifications
#     }
# }

# To manage users, run: python manage_users.py 
